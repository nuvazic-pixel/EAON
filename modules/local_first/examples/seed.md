# EAON seed evidence

A local-first architecture reduces unnecessary disclosure of private context to external services.
Tests indicate that explicit evidence references improve the auditability of generated answers.
A deterministic fallback ensures that the system can still retrieve evidence when the local language model is unavailable.
