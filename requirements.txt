# EAON 2.0 Dependencies
# ======================

# Core
python-dotenv>=1.0.0
structlog>=24.1.0

# HTTP
requests>=2.31.0

# Redis (optional - falls back to in-memory cache)
redis>=5.0.0

# GeoIP (optional - falls back to ip-api.com)
geoip2>=4.8.0

# Semantic routing (optional - falls back to keyword matching)
sentence-transformers>=2.2.0
numpy>=1.24.0

# Development
pytest>=8.0.0
pytest-cov>=4.1.0
black>=24.1.0
ruff>=0.2.0
mypy>=1.8.0
